"""
Simple example of work queues processing
Used standard rabbitmq configs, manual ack sending, declare queue as durable (can survive restart system)
and send persistent messages (messages will not be removed)
"""
